apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-pizza-app-deployment
  labels:
    app: my-pizza-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-pizza-app
  template:
    metadata:
      labels:
        app: my-pizza-app
    spec:
      containers:
      - name: my-pizza-app
        image: rudzmic1/cloudhours-workshops:0.1
        ports:
        - containerPort: 5000
          